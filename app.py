import streamlit as st

# --- 1. 全域設定 (Global Settings) ---
st.set_page_config(
    page_title="加密貨幣多維度分析系統",
    page_icon="🪙",
    layout="wide"
)

# --- 2. 側邊欄設定 (Sidebar) ---
st.sidebar.title("功能導航 🧭")

# 頁面選單
page = st.sidebar.radio(
    "前往頁面：",
    ["🏠 首頁 & 市場總覽", 
     "📰 新聞與輿情分析", 
     "📈 歷史回測系統", 
     "📊 技術分析室", 
     "💰 搬磚套利監控"]
)

st.sidebar.markdown("---")

# Demo 模式開關 (這是老師教你的作弊小技巧)
# 勾選後，系統會切換成「演示模式」，使用假數據確保 Demo 順利
use_demo_mode = st.sidebar.checkbox("啟動 Demo 演示模式", value=False)
if use_demo_mode:
    st.sidebar.success("🔴 目前為 Demo 模式 (使用模擬數據)")

# --- 3. 頁面路由邏輯 (Page Routing) ---
# 這裡負責判斷使用者選了什麼，就顯示什麼畫面

if page == "🏠 首頁 & 市場總覽":
    st.title("加密貨幣市場總覽")
    st.write("這裡將顯示：恐懼貪婪指數、比特幣即時價格、鯨魚警報...")
    # 未來我們會在這裡呼叫 ui_pages.dashboard.show()

elif page == "📰 新聞與輿情分析":
    st.title("即時新聞與情緒分析")
    st.write("這裡將顯示：Google News 爬蟲結果、情緒分數統計...")
    # 未來呼叫 ui_pages.news_page.show()

elif page == "📈 歷史回測系統":
    st.title("事件驅動回測")
    st.write("這裡將顯示：重大新聞發生後的幣價走勢圖...")

elif page == "📊 技術分析室":
    st.title("多維度技術指標")
    st.write("這裡將顯示：RSI, MACD, K線圖...")

elif page == "💰 搬磚套利監控":
    st.title("跨交易所套利監控")
    st.write("這裡將顯示：Binance vs OKX 價差表...")
    
    # 示範 Demo 模式的用法
    if use_demo_mode:
        st.metric("Binance - OKX 價差 (模擬)", "$150.23", "可套利!", delta_color="normal")
    else:
        st.info("正在連線交易所 API 獲取真實數據...")